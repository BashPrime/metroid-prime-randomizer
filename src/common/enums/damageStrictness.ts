/**
 * Enumerates options for damage strictness multipliers.
 *
 * These multipliers are used for calculating player damage during world traversal.
 */
export enum DamageStrictness {
  STRICT = 1,
  MEDIUM = 1.5,
  LENIENT = 2
}
